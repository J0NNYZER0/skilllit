'use strict'

module.exports = {
  Account: [
    {
      method: 'POST',
      path: '/login',
      handler: (request, h) => {
        
        h.response('Not yet implemented')
      }
    }
  ]
}
