const Routes = {
  Account: [
    {
      method: 'POST',
      path: '/login',
      config: {
        handler: (request, reply) => {
          reply('Not yet implemented')
        }
      }
    }
  ]
}

module.exports = {
  Routes: Routes
}
